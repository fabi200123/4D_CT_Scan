name: DoNothing

on: [push, workflow_dispatch] 

jobs:
  build:
    runs-on: [self-hosted, ubuntu]
    steps:
    - uses: actions/checkout@v3
    - name: Do Nothing
      run: |
        echo "Hello world!"
